**Static Site Generator**

A Python static site generator that converts Markdown files into HTML and outputs a finished website to the docs folder to be opened locally or published with GitHub Pages.

The generator reads Markdown content from the content folder, applies a shared HTML template, copies static assets like CSS and images, and recursively builds matching HTML pages in docs.